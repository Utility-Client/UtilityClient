package de.gamingcraft.overlay;import de.gamingcraft.UtilityClient;import de.gamingcraft.overlay.modules.CPSModule;import de.gamingcraft.overlay.modules.CoordsModule;import de.gamingcraft.overlay.modules.FPSModule;import net.minecraft.client.gui.FontRenderer;import net.minecraft.util.EnumChatFormatting;public class ModuleHandler {    private static IModule[] modules = new IModule[] {            new FPSModule(),            new CPSModule(),            new CoordsModule()            // new PingModule()            // new BiomeModule()    };    private static int op_x = 4, op_y = 4, mod_height = 9;    public static void start() {    }    public static void loop(FontRenderer fr) {        fr.drawStringWithShadow(UtilityClient.CURRENT_THEME.getPrefix() + UtilityClient.getClientName() + " " + UtilityClient.getVersion(), 4, 4, 0);        int x = 1;        for (IModule mod : modules) {            fr.drawStringWithShadow(UtilityClient.CURRENT_THEME.getPrefix() + mod.getName() + EnumChatFormatting.GRAY + ": " + UtilityClient.CURRENT_THEME.getSuffix() + mod.getValue(), op_x, op_y + (mod_height*x), 0);            x++;        }    }}